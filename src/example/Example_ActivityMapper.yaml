## result map
result_map id=BaseResultMap,table=biz_activiy:
  id c=id,p=id:
  result c=name,p=name,string:
  result c=pc_link,p=pc_link,string:
  result c=h5_link,p=h5_link,string:
  result c=remark,p=remark,string:
  result c=version,p=version,string,v=true:
  result c=create_time,p=create_time,time:
  result c=delete_flag,p=delete_flag,number,l=true,l_undelete=1,l_deleted=0:


## selectByIds
select selectByIds:
  sql:
    select * from biz_activity
    where delete_flag = 1
    and delete_flag = 2
  where:
    if id != null:
    #{id}
    if create != null:
      and create_time = #{create_time}
    if ids!= null:
     trim ',':
      for index,item in ids:
        sql:
        #{item}
        if id != null:
        #{id}